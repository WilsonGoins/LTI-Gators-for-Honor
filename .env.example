# =============================================================================
# Canvas SEB LTI Tool - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# --- LTI Platform Configuration ---
# These values come from Canvas when you create a Developer Key
LTI_PLATFORM_URL=http://YOUR_CANVAS_IP:3000
LTI_CLIENT_ID=YOUR_DEVELOPER_KEY_CLIENT_ID
LTI_AUTHENTICATION_ENDPOINT=http://YOUR_CANVAS_IP:3000/api/lti/authorize_redirect
LTI_ACCESS_TOKEN_ENDPOINT=http://YOUR_CANVAS_IP:3000/login/oauth2/token
LTI_KEYSET_ENDPOINT=http://YOUR_CANVAS_IP:3000/api/lti/security/jwks

# --- Tool Configuration ---
# The public URL where this LTI tool is accessible
# During local dev, this might be an ngrok URL or localhost
TOOL_URL=http://localhost:3001
TOOL_PORT=3001

# --- Database ---
# ltijs needs a MongoDB instance for session/key storage
# You can use a free MongoDB Atlas cluster or run one locally
MONGODB_URI=mongodb://localhost:27017/canvas-seb-lti

# --- Security ---
# Random string used to encrypt ltijs cookies/sessions
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
LTI_KEY=CHANGE_ME_TO_A_RANDOM_SECRET

# --- Canvas API ---
# Access token for Canvas REST API calls (quiz creation, etc.)
# You'll get this from Canvas Admin > Developer Keys
CANVAS_API_URL=http://YOUR_CANVAS_IP:3000/api/v1
CANVAS_API_TOKEN=YOUR_CANVAS_API_TOKEN

# --- SEB Defaults ---
SEB_DEFAULT_QUIT_PASSWORD=
SEB_DEFAULT_ALLOWED_DOMAIN=YOUR_CANVAS_IP:3000
